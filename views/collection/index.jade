extends ../layout

block content
  div(role='main')
    include ../layout/search

      .padpage.padtop
        h1 
          a.back.ui-link(href='/')
            img(src='/img/icon-back.png',alt='Back')
            | The Dua collections

        p
          | We have incredible riches of supplications in the narrations of the Ahl al-Bayt (AS) and subtle 
          | concepts in the whispered prayers (munajat) transmitted to us from them.

        .clients
            ul(data-role='listview')
              each row in data
                li
                  a(href='/duas/collection/#{row.collection}')
                    img.ui-li-icon(src='/img/sample-images/icon2.png', alt='duas')
                    | Supplications from #{row.collection} 

